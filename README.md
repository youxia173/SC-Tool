# SC 小工具 v1.2.0

一款用于《星际公民》（Star Citizen）的中文输入与截图工具，提供中文输入叠加窗口、IME 词表转译、剪贴板粘贴发送、可选中英双语发送、游戏内截图、自定义快捷键和系统托盘等功能。

按下唤醒键后，程序会同时打开游戏聊天框和中文输入窗口。使用系统输入法完成中文输入后，再次按下 `Enter` 即可转译并发送内容。开启「双语发送」时，还会调用所选翻译接口生成英文，并与中文转译一并粘贴发送。

## 项目来源

本项目是在 [BetterSCIME](https://github.com/typemiao/betterscime) 基础上修改和扩展的衍生项目。

BetterSCIME 提供了中文输入窗口、固定 `F8` 快捷键唤醒、IME 词表转译，以及通过模拟逐字键盘输入向游戏发送内容等基础功能。

本项目在 BetterSCIME 的基础上主要进行了以下修改和扩展：

- 增加快捷键设置功能；
- 将默认唤醒键调整为 `Enter`，使游戏聊天输入更加便捷；
- 支持设置其他按键作为输入窗口唤醒键；
- 将模拟逐字键盘输入改为通过剪贴板粘贴，提高发送效率和准确性；
- 增加游戏内截图到剪贴板功能；
- 增加截图快捷键设置功能；
- 增加系统托盘图标和右键菜单；
- 增加设置入口和软件退出功能；
- 调整并优化输入窗口及相关交互流程；
- 增加可选的中英双语发送（百度翻译 / 腾讯云 / 阿里云）；
- 设置中可切换翻译引擎、填写密钥，并提供开通与密钥获取链接及免费额度说明；
- 双语内容以一条消息、换行拼接的方式发送（`[zh] …` 换行 `[en] …`）；
- 每次启动软件时自动打开设置界面。

感谢 [BetterSCIME](https://github.com/typemiao/betterscime) 原作者 [typemiao](https://github.com/typemiao) 提供的基础代码和实现思路。

BetterSCIME 原有内容的版权归原作者及相关贡献者所有。本项目新增和修改的部分由本项目作者开发与维护。

## 主要功能

- 使用系统输入法在叠加窗口中输入中文；
- 根据 IME 词表将中文内容转译为游戏可用文本；
- 通过剪贴板将转译后的内容粘贴至游戏聊天框；
- 可选中英双语发送：中文转译与英文翻译在同一条消息中换行发送；
- 支持百度翻译、腾讯云机器翻译、阿里云机器翻译三种引擎；
- 支持自定义输入窗口唤醒键；
- 支持将游戏画面截图到剪贴板；
- 支持自定义截图快捷键；
- 提供系统托盘图标、设置入口和退出功能；
- 启动时自动打开设置界面，便于首次配置。

## 使用方法

1. 运行 `SC-Tool.exe`，程序图标会出现在系统托盘中，并自动打开设置窗口。
2. 进入《星际公民》，按下输入窗口唤醒键，默认为 `Enter`。
3. 程序会打开游戏聊天框，同时显示中文输入窗口。
4. 切换至中文输入法并输入内容。
5. 再次按下 `Enter`：
   - 输入框中有内容时，程序会转译文本，通过剪贴板将内容粘贴至游戏聊天框并发送，然后关闭游戏聊天框和输入窗口；
   - 若已开启「双语发送」且密钥配置正确，同一条消息中会包含中文转译与英文翻译（中间换行）；
   - 输入框中没有内容时，程序不会发送消息，而是直接关闭游戏聊天框和输入窗口。
6. 按下 `Esc` 或点击输入窗口以外的区域，可以关闭输入窗口。
7. 在游戏中按下截图键，默认为 `F10`，可以将当前游戏画面截图到剪贴板。
8. 右键单击托盘图标并选择“设置”，可以修改唤醒键、截图键，以及双语翻译相关选项。
9. 右键单击托盘图标并选择“退出”，可以结束程序。

> 当设置的唤醒键不是 `Enter` 时，程序会自动模拟一次回车，以打开游戏聊天框。

## 双语发送（可选）

在设置中勾选「双语发送」，在 **翻译引擎** 下拉框中选择其一并填写对应密钥，可用「测试翻译」验证：

| 引擎 | 密钥字段 | 大致免费额度（以官方为准） |
| --- | --- | --- |
| 百度翻译 | APP ID / 密钥 | 身份认证后最高约每月 200 万字符 |
| 腾讯云翻译 | SecretId / SecretKey | 文本翻译每月约 500 万字符（需领取资源包） |
| 阿里云翻译 | AccessKeyId / AccessKeySecret | 通用版文本翻译每月约 100 万字符 |

设置界面中提供了各引擎的开通与密钥获取链接。发送时格式示例：

```text
[zh] …
[en] …
```

翻译失败时仍会发送中文转译；正常模式下会以短提示告知失败原因。

## 配置文件

程序配置保存在 `SC-Tool.exe` 所在目录下的 `SC-Tool.cfg` 文件中，包括快捷键、截图提示选项、是否开启双语发送，以及所选翻译引擎与密钥等。

## 构建

构建本项目需要安装 Rust 工具链。

在项目目录中执行：

```sh
cargo build --release
```

构建产物位于：

```text
target/release/SC-Tool.exe
```

## 词表来源

本项目使用的 IME 转译词表继承自 [BetterSCIME](https://github.com/typemiao/betterscime)。

根据 BetterSCIME 原作者在原项目中的说明，该 IME 转译词表来源于 [StarCitizenToolBox/app](https://github.com/StarCitizenToolBox/app)。

词表来源关系如下：

```text
StarCitizenToolBox/app
        ↓ IME 转译词表
BetterSCIME
        ↓ 继承使用
SC 小工具
```

感谢 StarCitizenToolBox/app 的作者及贡献者提供相关 IME 转译词表。

词表原始内容的版权归相应作者及贡献者所有。本项目对相关词表的使用、修改和再分发遵循其原始许可证及版权声明。

## 免责声明

本项目为非官方工具，与 Cloud Imperium Games、Roberts Space Industries 及《星际公民》官方不存在隶属、授权、认可或合作关系。

使用者应自行判断并承担使用本工具可能产生的风险和后果。在适用法律允许的范围内，本项目作者及贡献者不对因使用或无法使用本工具而产生的任何直接或间接损失承担责任。

《Star Citizen》《星际公民》及相关名称、标志和商标归其各自权利人所有。

## License

本项目采用 [GNU General Public License v3.0](LICENSE) 许可证发布。

本项目基于 BetterSCIME 修改和扩展，并继承使用了来源于 StarCitizenToolBox/app 的 IME 转译词表。原项目代码、IME 转译词表及其他第三方内容，仍分别受其原始许可证和版权声明约束。

再分发本项目或其修改版本时，请同时保留适用的许可证、版权声明和来源说明。具体来源请参阅本 README 中的“项目来源”和“词表来源”章节。
